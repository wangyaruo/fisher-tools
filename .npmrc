# 使用扁平 node_modules 布局，而非 pnpm 默认的 isolated（符号链接）布局。
#
# 原因：默认布局依赖大量符号链接，在受限沙箱/托管环境中会被文件系统代理拒绝
# （ERR_PNPM_CODEBUDDY_BROKER_DENY），导致安装中断。
#
# 取舍：hoisted 布局失去了 pnpm 的严格依赖隔离，理论上会出现「幽灵依赖」
# 而不会被立刻发现。本项目为私有 monorepo，以兼容性换取可安装性。
# 若迁移到不受限的 CI 环境，可删除本文件恢复默认布局。
node-linker=hoisted
